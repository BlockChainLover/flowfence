# Data

Use this folder for local task manifests, policies, secrets, and prompt templates that define the benchmark inputs.

Expected subfolders:

- `data/tasks/`
- `data/policies/`
- `data/secrets/`
- `data/prompts/`

TODO: write the first smoke task manifest and policy files here.
