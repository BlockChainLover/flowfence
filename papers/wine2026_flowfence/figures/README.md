# Figures

`flowfence_pipeline.png` is the current Figure 1 architecture figure. It illustrates the FlowFence runtime containment pipeline: agent events are intercepted, risk-scored using runtime-observable signals, routed to safe-view outputs or quarantine, and recorded in an event log / propagation graph.

Before final submission, consider redrawing this as a vector figure to improve print quality and reduce rasterization artifacts.
