# Isolated dependency and initialization validation

Environment: /tmp/flowfence_gate_ac_env, Python3.10.9, compatible with MARBLE>=3.9,<3.12.95 installed distributions. Direct versions were read from canonical poetry.lock; transitive resolution constrained to that same lock. Added ruamel.yaml0.18.6 (imported by benchmark but undeclared in pyproject); its C extension is in the resolved lock. No source import was bypassed or removed. Initial unconstrained transitive leftovers were removed with uv pip sync before final checks. uv pip check passes.

Files: GATE_AC_DIRECT_REQUIREMENTS.txt and GATE_AC_ENVIRONMENT_LOCK.txt. Recreate with uv venv --python3.10 PATH, then uv pip sync --python PATH/bin/python GATE_AC_ENVIRONMENT_LOCK.txt. Final installed versions include litellm1.62.1, openai1.65.3, pydantic2.10.6, torch2.6.0, transformers4.49.0, ruamel.yaml0.18.6. Upstream's old aiohttp3.11.13 is yanked for a known regression; retained for fidelity, no production deployment implied.

Real full imports PASS: Engine, ResearchEnvironment, DBEnvironment, CodingEnvironment, Evaluator and all their imported dependencies.600 real Engine/environment/evaluator/adapter constructions PASS (300×2). Research and Coding construction are local. Database constructor executes its original code with subprocess.run, psycopg2.connect/cursor and requests.get replaced by explicit deterministic doubles; SQL is counted but not executed by PostgreSQL. This is mock-backed initialization, not proof of Docker images, anomaly timing, operating system dependencies or network reproduction. Original wait_for_alerts immediately returns True in this pin.

The sandbox stages the unmodified task into the existing fixed coding_config path and supplies path aliases so original Coding tool/evaluator files resolve; no source path patch. macOS case-insensitive path collision was corrected only in the fixture layout. Constructor config fills empty environment defaults and uses5 max iterations; no stopping code changes. Actual STAR scheduler is separately exercised with mocked planner choices and fails on the original unescaped rating braces. Module import PASS must not be confused with full workflow PASS.

All target provider entry points default to failure unless explicitly replaced by static response fixtures. Socket connect and create_connection are denied; recorded network attempts0. LiteLLM local cost map, HuggingFace offline and Transformers offline prevent import-time network fetches. No model weights instantiated or target generation performed. Optional token counting is replaced by the same zero-count double for both arms; no token-cost measurement claim.

Direct original Research/Coding utility evaluator methods are invoked with static responses; Database original batch_eval.py is invoked separately with a static result fixture and fake completion. No judge prompt, parsing threshold or scoring rule is edited. Their fabricated fixture scores are neither saved as scientific results nor used for selection.
