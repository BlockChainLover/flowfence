# Gate A-C pre-result eligibility predicate

Recorded before certification on2026-09-18. Machine predicate: PUBLIC_TASK_ELIGIBILITY_PREDICATE.json; record schema: PUBLIC_TASK_ELIGIBILITY_SCHEMA.json. Each criterion is true/false/null. ELIGIBLE requires every criterion true; INELIGIBLE requires a demonstrated failure; UNRESOLVED means missing evidence with no demonstrated failure. No unimplemented adapter or untested prerequisite is itself evidence that a task is intrinsically ineligible.

E2 candidate route is official STAR for all three environments, because this is an existing route containing Coding evaluation. This is a documented Gate A configuration proposal, not a scientific run or approval of E4-B. No custom Coding GRAPH evaluator. Original task and evaluator judge/settings retained. Model providers are never invoked here. Infrastructure mocks certify only the corresponding interface, not actual Docker or live research retrieval.

All15 criteria (splitting A/B checks explicitly) are in the predicate. Eligibility cannot depend on observed defense/utility performance. Sidecar values, role choice and contamination text are deterministic from public IDs and do not use gold answers. No raw value is required by the original task because it is newly introduced private metadata, not a replacement for task data. This establishes clean independence, not scientific attack efficacy.

After all300 records are audited, an environment needs≥23 ELIGIBLE records to select3 development and20 confirmation. Sort official IDs numerically. Within confirmation assign P1–P4 cyclically,5/class/environment,15/class overall. If that assignment fails semantic feasibility, stop; do not lower quotas. Development never enters confirmation. Unknowns do not count as eligible.
