DRAFT / Gate A-R audit — parser repair complete; benchmark integration remains NOT_READY. No development or confirmatory execution authorized.

# Deterministic selection proposal

After a usable pin and a complete engineering audit, enumerate each official manifest in file order (one-based official_order); identify tasks by (environment, task_id), assert unique IDs, and record every rejection with a pre-outcome reason. Static parsing alone is insufficient eligibility evidence. Require runnable initialization, original evaluator availability, observable/instrumentable transitions, capability parity and task-preserving augmentation.

For each environment, filter by established eligibility and sort by official_order, breaking any tie by string task_id. Reserve the first 3 as development; the next 20 as confirmation. If fewer than 23 are eligible, stop. No substitute based on defense performance, clean utility, attack success or observed failures. Record the ordered eligibility decision and selected IDs in version control before Gate B. Do not freeze a confirmatory protocol now.

Within each 20-task confirmatory list, index from 1. Assign P1/P2/P3/P4 cyclically, giving 5 per class per environment and 15 per class overall, conditional on independently documented semantic suitability. If suitability fails, stop and revise before development rather than searching for favorable outcomes.

Cross-model and topology subsets both use confirmatory ranks 1–6 in each environment. They may overlap each other, never development. These are subsets, not six new tasks. A more balanced six-task class allocation would require a predevelopment revision, not a post-result substitution.

Current state: 300 candidate rows inventoried, no eligibility pool or actual dev/confirm split established. Candidate booleans are false because selection has not occurred; no results fields exist.

## Gate A-R eligibility outcome

300 records pass structural checks,0 currently satisfy every integration criterion; no selection executed. Official ordering procedure remains unchanged. Research duplicate objectives8/91 and41/57 need semantic-unit review before establishing the pool. Database repeated task wording is not a duplicate test:100 full task definitions differ in root causes and environment settings. Never silently exclude by outcome or treat a textual duplicate as a new independent semantic task. Resume requires an approved eligibility pool before this deterministic algorithm can fix actual IDs.
