# Remote Inputs

Historical P1 raw traces named by the canonical manifests were not found on the remote host. The experiments therefore use the synced current implementation and task/policy definitions, while the historical committed high-level summaries remain read-only context. New raw deterministic traces remain in the isolated remote run roots.
