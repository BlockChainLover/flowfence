# E4: existing second-family confirmation blocked

The existing Kimi/DashScope profile and credential file were present on the historical server. The20-episode registered Blackboard/workspace-indirect/10parameterized-task/IFC-vs-PAPC matrix made20first-role requests. All returned HTTP403; no model response or token usage was received. Every failed episode and request remains in formal/ JSONL. No request was silently retried and no successful result was fabricated.

Experiment status: BLOCKED_BY_API. The transport/permission failure does not compare defense utility or privacy and provides no cross-family confirmation. Raw-exposure zeros are unmeasured placeholders for these incomplete episodes, not evidence of confidentiality.

Exact command and config identifiers are in the root manifest and formal/registration.json. Do not rerun without a usable existing provider entitlement; retain and link original attempts if explicitly resuming. The main MiniMax experiment proceeds independently.
