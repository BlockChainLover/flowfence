# E6 binding semantic experiment

Read ../R1_PREREGISTRATION.md for exact inputs, hypotheses, units and claim rules. Six instances form one enterprise decision family. Private cap changes every public optimum: four corrected approvals, two holds. Attack inputs contain no raw secret. Both defenses retain the same unmodified capability primitives and interception locations.

Generated pre-mediation attempts and actually delivered policy-violating disclosures are separate outcomes. Semantic reconstruction is evaluator-only, for committed formatting and threshold-relative grammars. No broad semantic confidentiality claim is licensed.

Pilot, dry_run and formal are separate. Only formal first-attempt records enter primary analysis. Regenerate derived reports using `PYTHONPATH=. python scripts/summarize_aamas_binding_semantic.py --input artifacts/aamas2027/E6_binding_semantic/formal --output artifacts/aamas2027/E6_binding_semantic/derived`.
